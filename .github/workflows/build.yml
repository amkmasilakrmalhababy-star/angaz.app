name: Flutter Auto Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          
      # التعديل هنا: إجبار فلاتر على اسم مشروع متوافق وإعادة الكتابة فوق الإعدادات لتجنب التعارض
      - name: Generate Android Structure
        run: flutter create . --platforms=android --project-name=enjaz_app --overwrite

      - name: Install Dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK Result
        uses: actions/upload-artifact@v4
        with:
          name: Enjaz-App
          path: build/app/outputs/flutter-apk/app-release.apk
